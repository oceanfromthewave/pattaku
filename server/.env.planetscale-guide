# ================================
# PlanetScale 설정 가이드
# ================================
# 
# 1. planetscale.com에서 가입
# 2. pattaku 데이터베이스 생성 (Seoul 리전)
# 3. Connect > Create password로 연결 정보 생성
# 4. 아래 정보를 실제 값으로 교체
#
# PlanetScale에서 제공하는 정보:
# Host: aws.connect.psdb.cloud (또는 다른 호스트)
# Username: [생성된_사용자명]
# Password: [생성된_비밀번호]  
# Database: pattaku
#
# ================================

# 로컬 개발용 (기본 설정)
DB_HOST=localhost
DB_USER=pattaku
DB_PASSWORD="pattaku12#$"
DB_NAME=pattaku
JWT_SECRET=패타쿠초강력비밀키

# 프로덕션용 (PlanetScale) - 실제 값으로 교체 필요
# DB_HOST=aws.connect.psdb.cloud
# DB_USER=실제_사용자명_입력
# DB_PASSWORD="실제_비밀번호_입력"
# DB_NAME=pattaku
# JWT_SECRET=패타쿠초강력비밀키

# ================================
# 설정 완료 후:
# 1. 위 주석(#) 제거하고 실제 값 입력
# 2. 로컬 설정은 주석 처리
# 3. npm start로 서버 재시작
# ================================